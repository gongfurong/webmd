# 幂等（Idempotency）

### 缩写：无

### 简述

同一操作执行多次与一次业务效果等价。让「至少一次」投递/重试变得安全；不等于消息系统宣称的 exactly-once。

### 使用场景

支付下单、消息消费、可重试写接口。

### 实践与应用

```text
同一请求执行 1 次与 N 次 → 业务效果相同
写操作重试前先确认幂等或带幂等键
```
### 注意事项

• 先查后改无原子性有竞态。
• 只在网关去重但下游仍非幂等会漏。

### 关联术语

• 重试：需要幂等保护的操作
• 事务：本地原子与跨请求幂等不同层
• API：写接口的设计约束
