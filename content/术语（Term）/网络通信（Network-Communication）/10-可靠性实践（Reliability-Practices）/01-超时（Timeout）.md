# 超时（Timeout）

### 缩写：无

### 简述

等待超过约定时间则中止。分布式止损基本件；过短误杀、过长占满资源。应分层并外短内更短。

### 使用场景

连接/读写/整体 deadline、熔断输入。

### 实践与应用

```text
设置超时 → 到期失败/取消 →（可选）重试
超时过长占资源；过短误杀慢成功
```
### 注意事项

• 库默认无限等待是隐患。
• 与调用链深度强相关。

### 关联术语

• 重试：超时后的常见后续
• 幂等：可安全重试的前提
• RTT：设置超时的参考
