# 重试（Retry）

### 缩写：无

### 简述

失败后有限次再试以挺过瞬时故障。必须：可重试条件、上限、退避+抖动、与幂等配合；禁止多层同时狂重试。

### 使用场景

幂等读、带幂等键的写、客户端 SDK。

### 实践与应用

• 只对明确安全的错误码自动重试。
• 尊重 Retry-After；平台级只保留一层主重试。

### 注意事项

```text
// 反例：无上限、无退避、非幂等写操作盲目重试
// 宜：有限次数 + 退避 + 幂等/去重
```
### 关联术语

• 超时：触发重试的常见原因
• 幂等：避免重复副作用
• 退避：降低雪崩的策略
