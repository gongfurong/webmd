# 端口（Port）

### 缩写：无

### 简述

传输层 16 位编号，用于区分同一主机上的不同应用/会话；与 IP 组成套接字地址。知名端口是惯例，不是保密手段。

### 使用场景

监听、安全组放行、容器映射、端口占用排查。

### 实践与应用

```text
套接字地址 ≈ (IP, 端口)
知名端口：如 80/443（以实际部署为准）
```
### 注意事项

• 开放端口=攻击面。
• 改非常用端口≠安全。

### 关联术语

• 套接字：地址与端口的编程抽象
• IP：主机定位
• 传输协议：端口所属的传输语义
