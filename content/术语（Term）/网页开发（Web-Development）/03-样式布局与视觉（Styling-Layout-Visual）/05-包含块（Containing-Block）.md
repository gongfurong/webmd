# 包含块（Containing Block）

### 缩写：无

### 简述

计算尺寸与偏移时的参照矩形；绝对定位与部分百分比相对包含块，不一定是直接父元素。

### 使用场景

绝对定位弹层、百分比高度、sticky 理解。

### 实践与应用

• 为绝对子元素建立定位祖先
• 注意 transform 对 fixed 的影响

### 关联术语

• 定位：依赖包含块
• 盒模型：被计算对象
